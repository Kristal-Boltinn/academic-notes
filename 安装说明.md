# Academic Notes 安装

完整用法见 [README.md](README.md)。

1. 将 Release 的 `main.js`、`manifest.json`、`styles.css` 放入 `.obsidian/plugins/academic-notes/`。
2. 在 Obsidian 的第三方插件设置中启用 Academic Notes。
3. 数学框、图表布局、编号和引用在桌面及 iPad 上可用；PDF 导出仅在桌面版显示，无需额外安装运行环境或 CSS 片段。

开发源码使用 `npm ci`、`npm run build`。更新时保留自己的 `data.json`，重新加载插件。
